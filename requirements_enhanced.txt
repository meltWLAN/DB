# 基础依赖
numpy>=1.20.0
pandas>=1.3.0
matplotlib>=3.4.0
tushare>=1.2.80
psutil>=5.8.0
aiohttp>=3.8.0
requests>=2.26.0
pyyaml>=6.0

# 阶段一优化依赖（缓存优化、多进程、向量化计算）
joblib>=1.1.0
cachetools>=5.0.0
numba>=0.54.0

# 阶段二优化依赖（异步数据获取、内存管理）
asyncio>=3.4.3
aiofiles>=0.8.0
uvloop>=0.16.0; platform_system != "Windows"
aiomultiprocess>=0.9.0

# 阶段三优化依赖（优化数据存储、分布式计算）
dask>=2023.1.0
distributed>=2023.1.0
pyarrow>=8.0.0
h5py>=3.7.0
lz4>=4.0.0
blosc>=1.10.6
redis>=4.3.4
msgpack>=1.0.4
sortedcontainers>=2.4.0
zict>=2.2.0
cloudpickle>=2.2.0

# 可选依赖
ipython>=8.0.0  # 用于交互式探索
plotly>=5.5.0   # 用于高级可视化
pytest>=7.0.0   # 用于单元测试
pytest-asyncio>=0.18.0  # 用于异步测试
pytest-benchmark>=3.4.1  # 用于性能测试 